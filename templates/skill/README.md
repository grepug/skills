# Skill Template

Copy this folder into `skills/<your-skill-name>/` and replace the example content.

Keep `SKILL.md` concise, actionable, and specific enough that an agent can follow it without inventing steps.
